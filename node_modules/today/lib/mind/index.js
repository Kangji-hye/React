'use strict';

const level = require("level-browserify");
const levelgraph = require("levelgraph");
const os = require('os');
const path = require('path');

// Uses a hexastore to create a S:P:O graph, a la RDF
//
const graph = levelgraph(level(path.join(os.homedir(), `.db-today`)));

/*
var triple = { subject: "a", predicate: "b", object: "c", "someStuff": 42 };
graph.put(triple, function() {
    graph.get({ subject: "a" }, function(err, list) {
        console.log(list);
    });
});
*/

module.exports = {
    add,
    search,
    remove
};

function add(triples=[]) {

}

function search(triples=[]) {

}

function remove(triples=[]) {

}